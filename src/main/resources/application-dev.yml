# 开发环境特定配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmaster_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=CONVERT_TO_NULL&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

  data:
    redis:
      host: 127.0.0.1
      port: 6379

logging:
  level:
    com.bigcabbage: debug